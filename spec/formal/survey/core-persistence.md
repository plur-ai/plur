# Survey: core persistence, concurrency and lifecycle

Survey agent report, condensed by the coordinator on 2026-09-23 (all line references and claims kept).
Leads, not findings. #1 and #2 were replayed by the survey agent in a scratchpad (tsx probes) — to be re-replayed by the modelling agent before counting. Paths relative to packages/core/src/.

1. **Git sync never pulls while any scope:local engram exists (S–M, REPLAYED by survey)** — sync.ts:507-518 stageStrippedEngrams, 547-562 commitChanges, 570-600 pullRebase, 647-707 sync State 3. Claims sync.ts:551-554, 694 ("resolve locally and retry"), sync.test.ts:312 ("no data loss"). HEAD = strip(W) so W is permanently dirty; `pull --rebase` → "cannot pull with rebase: You have unstaged changes"; `pull --no-edit` → "Your local changes ... would be overwritten by merge. Aborting"; result {"action":"synced","message":"Synced. NOT pulled — still 1 commit(s) behind…"}; retry identical; pushes then non-fast-forward. Tests sync.test.ts:237, 255 pull other files only. Also readEngramList accepts bare array (sync.ts:326) that parseEngramFile rejects (engrams.ts:115-116); sync.test.ts:262 writes one.
2. **Failed migration restores a stale backup over the live store (S, REPLAYED by survey)** — migrations/runner.ts:77-96 (no-clobber 86), 128-131 restoreBackup, 182, 187-200, 251-266. Claims 80-85, 178-181, 199. Backup .bak.<v> never refreshed; up() in memory so nothing written on throw, yet copyFileSync(.bak.<v>) over live (130), non-atomic, bypassing shrink guard. Replay: 1 entry → migrate (bak.0) → add 2 → rollback to 0 → rerun with failing 7th migration → 3 → 1 entries.
3. **Lock steal by rename lets two processes hold the lock (M, not replayed)** — store/async-lock.ts:204-288, 312-337 stealLock, 340-348; sync twin sync.ts:751-856. Claims 302-306, 309-310, 325-326. 3-process interleaving: A steals H and acquires; B renames A's live lock aside; C O_EXCL-creates; B's put-back fails; A and C both in critical section; A's releaseIfOurs leaves C's file.
4. **Stale threshold (60s) < longest legitimate hold (~90s sync), no heartbeat (fold into 3)** — async-lock.ts:74, 106, 185-190, 256; sync.ts:823. Claims 45-47, 53-54, 89-94. alive===undefined (other host / legacy token) stolen at 60s mtime.
5. **Postgres: failed advisory unlock returns lock-holding session to pool (S)** — storage-postgres.ts:1067-1098. Claim 1089-1091 contradicted by 794-797; release() without err (1096) returns to pool; pg_advisory_lock (1084) no timeout → permanent outage; re-entrant per session.
6. **Backup "unrecoverable" list counts non-engram ids (S)** — backup.ts:448, 486-487, 514-533. idsCreatedAfter ignores event type: co_injection INJ-… ids (index.ts:5531), '' from session_scope_changed (index.ts:10048), created-then-forgotten ids.
7. **Daily backups stop after one legitimate >10% removal (S)** — backup.ts:213-221, 275-291, 327-331. last_good_count updated only on success; no other writer → shrunk forever; rotation stops (332).
8. **Dedup UPDATE/MERGE locked check outside the lock (S)** — learn-async.ts:237-274, 276-312. Re-read under lock not re-checked; history old_statement stale (262).
9. **PGLite fingerprint size:mtimeMs (S)** — storage-pglite.ts:532-548, 569-633; claim 572-573 contradicted by store/yaml-primary-store.ts:9-13. Same-size writes in one mtime tick skip sync.
10. **orderBySupersedes with duplicate ids (S)** — outbox-order.ts:28-77; claims 27, 52-54, 70-71. byId last-wins (33), ready lists id twice (55), last pushed twice, first dropped. Caller may or may not pass duplicates (unverified). Good first proof: permutation + edges + stable under distinct-id precondition.
11. **Store-shape / duplicate-id rules drift across five readers (S)** — engrams.ts:98-161, sync.ts:317-333, backup.ts:191-209, storage-pglite.ts:653-661, storage-postgres.ts:956-1000. Claims engrams.ts:86-92, normalize-engram.ts:4-9.
12. **Shrink guard ratchet / id allocation (S–M)** — engrams.ts:344-407, 457-507, 642-676; history.ts:167-179; index.ts:1687-1720. Per-write 10% ratchets; countEngramsOnDisk null fails open (434-436); minted-id cache once/day/process (documented 1697-1701).

Not candidates: async-mutex, session-scopes, tension-store, episodes, config, receipt*, provenance-store, version-check, capability-canary, backend-selection, storage-indexed, pglite-embeddings-export.
Env note: in this environment `grep` is a shell function that treats UTF-8 sources as binary; use `/usr/bin/grep -a`.
