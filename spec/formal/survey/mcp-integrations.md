# Survey: MCP server and integrations (mcp, dsh, ui, claw, migrate, opencode, hermes, python, langchain)

Survey agent report, condensed by the coordinator on 2026-09-23 (all line references and claims kept).
Leads, not findings. Paths relative to packages/.

1. **MCP session lifecycle / per-session write scope (M)** — mcp/src/tools.ts:593-700, 2960-3070, 3367-3495, 3498-3650; core/src/session-scopes.ts:43-95. Claims tools.ts:3397, 642-645, 620-624; session-scopes.ts:31-32. Every start rewrites process slot (3058); two open sessions + no session_id → plur_learn falls back to last-started session's default (session-scopes.ts:66-71), while plur_session_scope refuses that ambiguity (3439-3445). Id-only sweeps (648, 690) delete telemetry row but not scope registration → leak forever (comment 622-623 false). session_end without id is a state no-op (3612). Unknown explicit id accepted (686-688).
2. **.plur.yaml scope trust: three adapters, three rules (S)** — dsh/src/workspace-scope.ts:36-58, dsh/src/scope.ts:47-81, mcp/src/tools.ts:3037-3064, opencode/src/scope.ts:62-91. Claims dsh/src/scope.ts:25, 36-38; opencode resolveTrustedScope docstring. dsh and MCP session_start adopt file scope unconditionally; `scope: global` → dsh scopes ['global'] contradicting scope.ts:25; MCP makes it session default (no isDirectoryTrusted in mcp/src).
3. **learn rule differs across plur_learn / plur_learn_batch / plur_session_end (S–M)** — mcp/src/tools.ts:1133-1370, 1430-1570, 3535-3600. Claims 1247-1249, 1833, 1024. Batch forwards pinned (1447) without pinnedQuota (only 1258, 1787, 1805); plur_learn q.free<=0 coarse (1259); session_end uses plur.learn not learnRouted (3587), no session/domain; batch ignores session & project domain; plur_learn reports decision 'ADD' unconditionally (1331, 1363); catch path warns "Remote write failed" for local writes (1350-1368).
4. **dsh containment (S)** — dsh/src/guard.ts:31-99, tools.ts:75-190, engine.ts:78-99, learn.ts:138-146, capture.ts:65-75. Claims tools.ts:124-127, learn.ts:140-142, guard.ts:79-80, learn.ts:109-110. Facade resolves undefined with core unloadable → "Stored."/"Retired." (tools.ts:128-136, 159-162); learn.ts:143 guard dead for facade; guard timeout releases queue slot while write continues; tool fns without inner timeout wedge the queue.
5. **Hermes bridge dedup scope-blind; tool paths hard-code global (S)** — hermes/plur_hermes/bridge.py:242-270, 402-474; memory_provider.py:713-727; __init__.py:372. Core claim index.ts:1850-1852; bridge.py:422-427, 38-40. forget doesn't touch cache (501); test_memory_provider.py:174 enshrines scope="global".
6. **plur_admin dispatch / annotations (S)** — mcp/src/tools.ts:720-750, 810-930; server.ts:229-315. Claims 871-879, 825-826. plur_tensions resolve (3910) and plur_rescope (3880) retire but not destructiveHint → dispatchable via plur_admin while plur_forget refused; plur_session_start readOnlyHint true (2962) yet flushes outbox/mutates registry; plur_tensions idempotentHint yet scan persists.
7. **Hermes/Python bridges: timeouts → success; statement text parsed as CLI flags (S)** — hermes/plur_hermes/bridge.py:296-450; python/plur_ai/client.py:39-64; cli/src/plur.ts:26-38. _SAFE_RESPONSE on exhaustion for writes (334), timed-out learn retried; returncode 2 success for any command (382); no `--` before statement (bridge.py:429, client.py:51) and expandEqualsFlags splits `--path=…`/`--scope=…`.
8. **migrate codemod edit positions / summary (S)** — migrate/src/scan.ts:630-762, index.ts:84-139. Nested fixes on one line: `x = plur.list(plur.getById(id)).length` → outer wrap end stale by 6; summary counts all fixable as fixed (120, 127) though applyFixes skips (748, 750, 754); report-only exits 0 with fixable outstanding (138).
9. **MCP array coercion comma-splits (S)** — mcp/src/tools.ts:270-320. engram_suggestions union → "Use pnpm, not npm" split (308) → "not npm" learned by session_end (3584-3592). integer→z.number() (279).
10. **opencode render-path latch / turn buffer (S)** — opencode/src/capability.ts, turn.ts, index.ts:104-230. Double injection when latched fallback + transform both fire; late snapshot re-arms buffer after takeIfFresh (turn.ts:49-51) → transcript learned twice; injectHybrid unbounded (index.ts:121).
11. **claw setup/repair merge; context-engine dedup (S)** — claw/src/setup.ts:69-176, 510-600; context-engine.ts:369-379. Claims setup.ts:531-532, 83-84; context-engine.ts:363-364. mergeEnable overwrites foreign slots.memory (113-116) on postinstall (609) vs repair only-if-absent (584); non-object plugins replaced by {} (70-72); seen.add before await learnRouted (376-377); session state leaks for top-level sessions.
12. **UI host-header policy (S, low)** — ui/src/server.ts:112-345. normaliseHostName not idempotent ("[[::1]]" → "[::1]" → "::1"), errs toward refusal; Host ":80" → "" allowed (263), not browser-sendable.

Not candidates: learner heuristics (hermes learner.py byte-identical to langchain; dsh learn.ts regexes diverged); mcp/src/index.ts init config (follows #1059); mcp/src/drop-log.ts.
