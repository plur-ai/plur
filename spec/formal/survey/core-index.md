# Survey: packages/core/src/index.ts (Plur class)

Survey agent report, condensed by the coordinator on 2026-09-23 (all line references and claims kept).
Leads, not findings: nothing here is CONFIRMED until replayed against the code. Lines are index.ts unless named.

1. **Outbox delivery protocol (M)** — learn remote branch + fire-and-forget push 3107–3230; learnRouted outbox fallback 3443–3480; flushOutbox 7748–8075; forget cancels outbox 6576–6584; rescope cancels 7044–7064. Claims: 6576–6579 (#766 retired must not be resurrected), 7752–7754, 3155–3159, 8020–8022 (merge-back writes only outbox metadata/demotion/scope), 7050–7054 (#848). Suspicions: flushOutbox snapshots unlocked (7750) and selects rows learn() queued with attempt_count 0 (3141–3149) whose background push (3169) is in flight → double append to remote; no "in-flight" marker; two concurrent flushes race likewise. Merge-back success arm (8033) drops pushed row unconditionally even if forget() retired it mid-push → #766 resurrection. Failure arm (8044–8047) copies survivor `_outbox` over a concurrent rescope/forget deletion → #848. learn background catch (3175–3183) sets attempt_count=1 absolutely. Tests: outbox*.test.ts, supersedes-flush-remap.test.ts, inject-counter-and-flush-merge.test.ts:113 (only concurrent statement edit on failed push).
2. **"Private engrams stay local" — three predicates (S)** — learn 3112–3118, learnRouted remote route 3400–3441, default visibility 3583–3588, sync.ts:345–350 pushKeep. Claims 3116–3117 (#90), sync.ts:340–343. learnRouted never checks visibility; learn checks only caller-supplied context.visibility, not resolved default 'private'; sync excludes private. No tests for #90.
3. **Invariant `_outbox ⇒ scope == _outbox.target_scope` kept only by rescope (S)** — updateEngram local 6049–6069 writes scope without touching _outbox; applyMutation cross-scope recurrence sets scope='global' (2209) on a row possibly carrying _outbox; flush pushes to outbox.target_scope (7834, 7971). Claim 7045–7051 (#848). Test: rescope-outbox-cancel.test.ts only.
4. **Readonly instance mutators without `_assertWritable` (S)** — recordTensions 8502, confirmTension 8584, dismissTension 8596, resolveTension 8609–8670. Claims 1038–1040, 1588. readonly.test.ts:98–112 omits them. resolveTension writes the claim before the guard fails, rollback swallowed (8662).
5. **Tension lifecycle and lock-escalation gate (S)** — 8584–8745, consumer 2191–2225. Claims 8587, 2195–2197, 8610–8625. hasUnresolvedTension `catch { return false }` (8734–8741): unreadable tensions.yaml → "no tension" → escalation to locked allowed (fails open). resolve rollback failure swallowed (8662).
6. **Auto-route + leak-guard pipeline (S)** — _resolveUnscopedScope 2704–2769, _guardSensitiveScope 2782–2852, previewAutoRoute 2489–2504. Proof targets: final shared ⇒ source ∈ {explicit, session} ∨ allow_shared_auto_route; offending ∧ (shared ∨ remote) ⇒ local; preview = write decision. Suspicion: learnRouted passes guarded.context with scope undefined; learn() re-resolves (3372→2955→2805) after reloadConfigIfChanged → `_routed` for scope A stamped (3385–3390) on an engram placed in B.
7. **forget/feedback ambiguity guard (S)** — 6483–6533, 5733–5774, 6733–6735. Any non-empty remote cache treated as authoritative (6499–6500, 5741–5742), freshness unchecked; no-probeById fallback maps getById null → 'absent' (6733–6735), the collapse 5874–5883 warns about. Risk #831 wrong engram retired.
8. **Reference-counted retirement #107 asymmetric (S)** — remote retire calls driver.remove without decrement (6424, 6755); _recordDuplicate persists increment only for primary (2122). Claims 6460–6462, 6596–6601.
9. **Egress guard stale config / scan surfaces (M)** — reloadConfigIfChanged only on unscoped path (2713), not explicit-scope learn or flushOutbox, contradicting "CURRENT policy" (7890–7897); _isRemoteBackedScope includes readonly URL stores (1802) while router skips them (1782), contradicting "mirrors" (1796–1798); updateEngram runs guard per store before ownership known (6102).
10. **Dedup / cross-scope recurrence swallows writes (M)** — _crossScopeRecurrenceDetect scans remote cache + packs (1366–1368, 1394–1418); hit applied to hit only (2291–2296) → write lost, contradicting 3670–3673; PG path skips primary recurrence (3033) vs YAML broadens to global (2209).
11. **rescope "atomic semantics" (S)** — claim 6807–6809; _retireRescopedSource throw after successful push (7014) leaves both copies active, unreported; vanished row returns silently (7114) yet appends engram_retired history (7131–7136).
