# Survey: packages/cli (`plur` CLI)

Survey agent report, condensed by the coordinator on 2026-09-23 (all line references and claims kept).
Leads, not findings. Paths relative to packages/cli/. Note: no hook/MCP uninstall command exists (COMMANDS src/index.ts:115-185; only `packs uninstall`), so all installers are merge-only.

1. **Claude settings.json hook merge (S–M)** — src/commands/init.ts:691-729, callers 1356-1425, shimPath 122-125. Claims init.ts:719 (idempotent), 1363-1364 (#1059); codex-hooks.ts:122-128, 143-146 state the per-spec rule. Suspicions: Windows shim path uses `\`, isPlurHook tests `.plur/bin/plur-hook` (693) → re-runs duplicate hooks, doctor.ts:217 same; strips whole entries not specs (708) vs codex per-spec (codex-hooks.ts:152-157); matches binary without subcommand (692-694) → user `npx @plur-ai/cli recall` hook deleted; `h.command.includes` throws on type:"prompt" hooks with no command, Claude leg not in containLeg (1374-1379) → init aborts; writeSettings non-atomic (1171-1174). Test init.test.ts:116 POSIX only; CI ubuntu-only.
2. **Cursor hooks.json drops top-level keys (S)** — src/cursor-hooks.ts:93-101, 104-116. Claim 108-109 (unknown keys survive). Line 100 returns `{ version, hooks }` only; codex returns `{ ...clean, hooks }` (codex-hooks.ts:174). Tests don't check extra keys.
3. **Stale MCP entry repair replaces env (S)** — src/mcp-config.ts:374-393, 404-420; Cursor caller init.ts:799-817. Claims 364-369, 381-384. Line 389 passes recommended.env with no merge → user PLUR_PATH lost. Test mcp-config-cursor.test.ts:102 only equal-env case.
4. **.plur.yaml scope from untrusted dirs (S)** — hook-inject.ts:353, 376-379, 413-431; hook-codex-inject.ts:48; hook-cursor-session-start.ts:71; hook-agy-pre-invocation.ts:129-135. Claims trust.ts:6-11, 76; hook-inject.ts:377-379 ("need no gate"); core project-remote.ts:31-36 (opencode gates scope). Untrusted cloned repo sets scope: group:acme/eng → hook dials registered remote for that scope with prompt text. Adapter disagreement; possibly intended → decision board.
5. **Exit codes report success on failure (S)** — feedback.ts:22-43 batch exits 0 on all-fail; forget.ts:111-116 JSON no-match exit 0 vs text 1; forget.ts:131-143 multiple matches exit 0; scopes.ts:43 --json register failure exit 0; contrast rescope.ts:66. Claims output.ts:180, 112-113, forget.ts:88-90. JSON is default when piped (output.ts:17-20).
6. **Session checkpoint lifecycle (M)** — hook-learn-check.ts:28-37, 64-106, 150-162; hook-session-end.ts:37-45, 71-170; hook-inject.ts:220-270 processDeferredWrapups. Claims hook-session-end.ts:96-104, 150-151, 38-40. Deferred wrap-up unlinks corrupt (261-262) and valid (259) checkpoints after a transient notice, no capture; live sessions idle >5 min treated as orphans (238-242); writer keys CLAUDE_SESSION_ID||ppid (28-30), closer tries payload session_id first (41); session-end honors flags.path, writer and scan only PLUR_PATH.
7. **Session identity + inject lock (M)** — hook-inject.ts:121-133, 350-374, 501. Claim 361-366. Marker never deleted, keyed by ppid while guard keys by session_id → /clear or recycled PID skips injection; lock stat-then-write without O_EXCL (370-374); lock not released on throw (448-458).
8. **Session guards ×4 and session-dir vetting (S)** — hook-session-guard.ts, hook-codex-guard.ts:37-75, hook-cursor-guard.ts:67-107, hook-agy-guard.ts:56-95; lib/*-hook-io.ts. Claims hook-session-guard.ts:33, 118-120; codex-hook-io.ts:96-105 (#1060 extended to every family); cursor-hook-io.ts:146-149. Cursor writes into a refused dir (151-152, 179-208) following planted symlinks; readers never vet (codex-hook-io.ts:176, agy-hook-io.ts:94, readAgyTurnCache 138-156) → planted turncache text emitted into model context (hook-agy-pre-invocation.ts:110); Claude family unhardened (plain mkdirSync, sentinel in tmpdir); Claude exempts only exact tool name vs suffix match elsewhere.
9. **init-remote .plur.yaml rewrite (S)** — init-remote.ts:111-160, 241-256, 391-394. Claim 20-21 idempotent. Header comments (151-152) accumulate each run; trim() strips nested remote_url; readRemoteFromConfig doesn't unquote vs core project-config.ts:94-96; home resolution differs (217 vs project-config.ts:66).
10. **doctor false green (S)** — doctor.ts:212-222, 840-844, 951-954, 1245-1279. hooksInstalled spans cursor/codex/agy files (842, 357, 370, 381) yet prints "ready to use in Claude Code".
11. **"Atomic" append-then-stat counters (S)** — hook-learn-check.ts:40-60; lib/cursor-hook-io.ts:169-193. Claim 40-54. Interleaving A-append, B-append, A-stat, B-stat → both observe 2; N-multiple skipped or double.
12. **Antigravity per-turn cache (S–M)** — hook-agy-pre-invocation.ts:74-187; lib/agy-hook-io.ts:129-167, 221-248. Claims 42-43, 47-51, agy-hook-io.ts:131-136. Missing step_index → -1 → identical re-sent message replays stale memory; unwritable cache → every turn is "first"; failed counter never nudges (fails open, consistent).
