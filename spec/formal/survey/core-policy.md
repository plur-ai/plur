# Survey: core policy, security, provenance, remote protocol

Survey agent report, condensed by the coordinator on 2026-09-23 (all line references and claims kept).
Leads, not findings. Paths relative to packages/core/src/.

1. **isLocalOnlyScope treats every project:* as local though project:* is routinely remote-backed (S)** — scope-target.ts:23-40, 56-71; consumers index.ts:5849, 6451-6454, 6686. Claims scope-target.ts:30; index.ts:6454; scope-util.ts:23 lists 'project:' as SHARED; remote-recall.ts:38-40 dials project:plur/… scopes. forget/feedback naming a url-backed project:x cannot reach it; error advises passing the scope already passed. Case drift: LOCAL_FAMILY case-sensitive, isSharedScope case-folds (scope-util.ts:32). Test local-scope-never-routes-remote.test.ts lacks url-backed project store.
2. **decideAutoRoute refuses shared only; remote-backed personal scopes also leave the machine (S)** — scope-routing.ts:464-488; index.ts:2704-2776, 2573-2605. Claims scope-routing.ts:397-401 vs index.ts:2552-2554. Unscoped write auto-routes into url-backed user:*/agent:*; covers may come from server /me (store/remote-store.ts:461-495); TRUST RULE (schemas/scope-metadata.ts:38-47) restricts only sensitivity. Tests use path-backed user: stores only.
3. **Remote-recall breaker keyed by URL, dialed per (url, token) (M)** — remote-recall.ts:273-285, 697-825, 865-872. Shared `h` per normalized URL (710): revoked + healthy token interleave resets forbidden_count; two 403 tokens in one call → forbidden after one round (785); 429 on one token parks the other; interleaving-dependent.
4. **Pack registry keyed by manifest name, dirs by basename (M)** — packs.ts:231-247, 1215-1223, 1356-1363, 1476-1512, 1547-1571. Claims packs.ts:215-220, 150-154. Two dirs with same manifest name share one row; uninstall removes row by both dir name and manifest name (1503-1504), wiping unrelated baselines → unverified.
5. **computePackHash not injective (S)** — packs.ts:2278-2294, verify 2234-2265. SHA256(SKILL.md ‖ engrams.yaml) unframed: bytes moved across the boundary preserve H; missing file = empty file; legacy manifest.yaml outside integrity. Claims packs.ts:2268-2274 ("content-addressable identifier").
6. **Server-supplied `_pack` survives remote ingestion (S)** — tensions.ts:395-429; index.ts:1374-1390; remote-recall.ts:586-600; RemoteRowSchema .passthrough() (store/remote-store.ts:20-36). Claim tensions.ts:396-398 ("can only look MORE foreign"), 418-421. Remote row with _pack of an installed pack gets that pack's origin → measured-under gate hides contradictions (#981); also dropped by withoutPacks (index.ts:5350, 7233), miscounted injected_packs (5428).
7. **Remote-row ingestion implemented twice (S/M)** — remote-recall.ts:560-620 vs index.ts:1362-1392. (a) remote-recall.ts:606-611 type-guarded storage_strength/frequency defaults then `...act` overwrites them with raw server values; (b) index.ts:1386 non-idempotent replace + global→each loading store vs namespaceEngramId + global→dialedEntries[0] (587-588): different namespaced ids for same row. Claims index.ts:~1353-1360, remote-recall.ts:549-552.
8. **Decay floor / monotonicity (S)** — decay.ts:7-14, 103-136; feedback.ts:78-89; inject.ts:443-452. Unparseable date → NaN; Math.max(0.1, NaN) = NaN violates "Floor at 0.1"; feedback floors 0.0 < decay FLOOR 0.05 → decayedStrength rises over time; shouldInject (decay.ts:34) family-prefix match contrary to isScopeWithin (#383), exported, no src caller.
9. **Provenance summary defaults fail open (S)** — provenance.ts:1129-1160, 1278-1345, 666-668, 369-428. may_leave_this_machine initial true (1140) stays true with no subject/scope/visibility vs "Default to NO" (1141); missing licenseSource read as 'chosen' (1280) skipping fail-closed branch (1340-1343); withheld/isPrivate test scope==='local' only.
10. **RemoteStore.load: malformed body trips host-down breaker (S)** — store/remote-store.ts:521-600, breaker 103-151. Claim 119-120 "HTTP responses never trip it"; body parse/iterate throw lands in catch 575 → markRemoteHostDown (577), logged as network failure; fetchBounded (339-351) deliberately does not.
11. **scope_source not bookkeeping, not validated (S)** — index.ts:3654-3662, store/remote-store.ts:646-658, content-fields.ts:96-117. _scopeSource absent from PLUR_BOOKKEEPING_KEYS (104-106) → scanned as content and settable via structured_data on update; forwarded as any string (653) → a replayed/updated engram can claim 'explicit'.
12. **Scope-family predicate drift umbrella (M)** — scope-util.ts:23-57, scope-target.ts:23-40, scope-routing.ts:477, index.ts:2577, provenance.ts:666, tensions.ts:252-255. Five answers to "does s leave the machine / is it private". Claim scope-util.ts:45-53. One shared Lean model: classification + consumer table + pairwise consistency.

Held on reading: secrets.ts scan_truncated offending; validity.ts monotone in now; jwt.ts display-only; trust.ts fails closed; project-remote.ts fails closed; sanitize idempotent; normalize-engram single rule; content-hash deterministic; nextCommitment never reaches locked.
