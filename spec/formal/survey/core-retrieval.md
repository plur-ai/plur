# Survey: core retrieval, injection, learning, telemetry (packages/core/src)

Survey agent report, condensed by the coordinator on 2026-09-23 (all line references and counterexamples kept).
Leads, not findings: nothing here is CONFIRMED until replayed against the code.

1. **Injection scope visibility bypass (M)** — inject.ts:376-406, 652-718, 842-885. scoreEngram returns 0 for both "scope-excluded" and "no hits"; three paths revive a scope-rejected 0: (a) pinned `raw > 0 || pinned` (686) + minRelevance exemption (740) + pinned-first fill (554-599); (b) embedding boost `raw===0 && embBoost>0.5` (667-668, 702-704), local boosts in injectHybrid restricted by options.scopes but not options.scope (index.ts:5186-5245); (c) spreading activation (852-883) checks status, not scope. Counterexample: scope='project:a', pinned engram in ungranted group:acme/other is rendered; under scope='global' a pinned user:x engram is delivered, contradicting INJECT_GLOBAL_IS_TARGETED. Tests: inject-scopes.test.ts, read-side-scope-visibility.test.ts do not cover pinned/boosted/associated. Bounded by options.scopes authorization (index.ts:5352-5357).
2. **Miss-signal (S)** — telemetry-miss-signal.ts:96-147, caller index.ts:4009-4022, PGLite index.ts:4211. (i) low_score unreachable: rrfMerge gives rank-0 1/61≈0.01639 > floor 0.015, comment claims opposite; (ii) PGLite always topScore:null with non-empty results → classified no_results (line 144), so every PGLite recall is a "miss"; also YAML path with empty local leg + remote rows; (iii) domain sent verbatim (158) though #312 kind-reduction rationale applies.
3. **Pinned origin priority across three passes (M)** — inject.ts:510-620, 764-785. Shared pinnedLedger, fresh skippedRank per pass (572). maxTokens=1000: primary pin P (450) skipped pass 1; pack pin Q (300) admitted pass 2; pass 3 P omitted as pinned-sub-budget (300+450>500). Claim inject.ts:236-238, 551-553, 568-571. Test pinned-quota.test.ts:163 covers a single fillTokenBudget.
4. **learner.ts polarity inversion (M)** — learner.ts:120-136, 199, 259-293. Unanchored `((?:always|never)\s+.+)` (131) precedes don't/do-not (132): "Don't always rerun the full suite" → "always rerun the full suite". Claims learner.ts:122-130, 188-191.
5. **Injection token budget (S–M)** — estimateTokens (inject.ts:269-287) omits `Kind:` (1057-1058), EXPIRED prefix (1028, 188), `(inferred) `; layer-1 summary untruncated (996, 1104); tokens_used = directives ≤ maxTokens + consider (≤200) + spread_budget (480) → exceeds injection_budget by ≥34%. Claims inject.ts:265-267, 1086-1088.
6. **renderMemoryBlock budget (S)** — memory-block.ts:55-94: sections appended whole when remainingBudget > 0 / > 100 left, never checked against section size. Claim memory-block.ts:46-48. Test memory-block.test.ts:19 covers remaining ≤ 0 only.
7. **Telemetry counters races (S–M)** — telemetry-counters.ts:154-209, 244-255; telemetry-flush.ts:123-162. No lock: two processes reading same stale snapshot S → pending = S+S (162-166), the exact scenario the comment (156-158) claims to handle; lost same-day increments; duplicate POSTs; resurrection after delete. Not replayed.
8. **Query rewrite drops non-ASCII (S)** — intent/rewrite.ts:86-110, 134-152. tokenCore strips [^a-z0-9] (87); "What is the 部署 process for kubernetes?" → "is the process for kubernetes"; 2-token guard (150) passes. Not replayed.
9. **Hybrid labels / limit cap (S)** — recallAuto reports strategy_used:'hybrid' when embedder failed (search-orchestrator.ts:64, 85-86, 96; hybrid-search.ts:126-127); expandedSearch returns max(limit,50) for aggregation (query-expansion.ts:142-143) vs "always capped at limit" (hybrid-search.ts:117) — may be intended.
10. **Importer dry-run vs real (M)** — importers/engine.ts:49-154: dry-run omits sensitivity guard and scope-dependent dedup; re-import bumps write_count despite "idempotent" report.
11. **Capsule integrity (S)** — capsule.ts:166-173: verifyCapsuleIntegrity true for any 64-byte signature, header not hashed; exported, unused internally.
12. **Embedding cache merge (S, low)** — embeddings.ts:337-353: existing entry wins without hash compare; self-heals. injectHybrid swallows errors → silent BM25-only inject (index.ts:5277-5279).

Dropped: fresh-tail undefined commitment (tested); draft/expired/retired exclusion in inject incl. spreading (enforced); telemetry payload content-free; BM25 tf/df share termMatches.
